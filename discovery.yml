server:
  port: 9001

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: https://github.com/skaferun9/configServer-SpringCloudAssignment